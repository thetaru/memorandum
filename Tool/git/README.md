# git
```
### ブランチ作成
$ git branch develop

### ブランチ切り替え(なかったら作成)
$ git checkout -b develop

### ブランチ切り替え(存在するブランチのみ)
$ git checkout develop

###
$ git log 

###
$ git status

###
$ git reset --soft <commitID>

###
$ git reset --hard <commitID>
```
